// material-ui
import { Typography } from '@mui/material';

// project import
import MainCard from 'components/MainCard';

const MissionFilledPage = () => {
  return (
    <MainCard title="Sample Card">
      <Typography variant="body2">
        MissionFilledPage
      </Typography>
    </MainCard>
  );
};

export default MissionFilledPage;
