// project import
import dashboard from './dashboard';
import profile from './profile';
import mission from './mission';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, profile, mission]
};

export default menuItems;
