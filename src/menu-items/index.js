// project import
import profile from './profile';
import mission from './mission';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [profile, mission]
};

export default menuItems;
